FOR STRUCTIONSITE UPLOADS:
  1. Check for Chromedriver's existence
     - **in terminal** chromedriver --version
  2. If no chromedriver
     - pip install --grade webdriver-manager
  3. If yes chromedriver
